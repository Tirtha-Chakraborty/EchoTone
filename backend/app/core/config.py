"""
Configuration and settings for the EchoTone backend.

This module will eventually define a Settings class (likely using Pydantic BaseSettings)
to load environment variables such as:
- OpenAI API key
- Spotify client credentials
- allowed CORS origins
and expose a singleton settings instance to be imported across the app.
"""

